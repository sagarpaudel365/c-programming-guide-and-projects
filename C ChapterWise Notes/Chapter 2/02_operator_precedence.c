#include<stdio.h>

int main() {
    int a = 4 ;
    int b = 5 ;
    printf("The value of 3*a - 4*b is %d \n",3*a - 4*b);
    printf("The value of 3*a / 4*b is %f \n",3.0*a / 4.0*b);
    // 3*4 / 4*5 would give wrong answer i.e 15. 
    return 0;
}